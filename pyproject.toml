[build-system]
build-backend = "hatchling.build"
requires = [ "hatchling" ]

[project]
name = "django-form-alpine"
version = "0.0.5"
description = "Integrate Alpine.js into Django forms declaratively using widget attrs and resolvers"
readme = { file = "README.md", content-type = "text/markdown" }
keywords = [ "alpine", "alpinejs", "django", "django-admin", "form" ]
license = "MIT"
license-files = [ "LICENSE" ]
maintainers = [ { name = "Rodolfo Valentín Becerra García", email = "rodolvbg@gmail.com" } ]
authors = [ { name = "Rodolfo Valentín Becerra García", email = "rodolvbg@gmail.com" } ]
requires-python = ">=3.9"
classifiers = [
  "Framework :: Django",
  "Framework :: Django :: 3.2",
  "Framework :: Django :: 4.0",
  "Framework :: Django :: 4.1",
  "Framework :: Django :: 4.2",
  "Framework :: Django :: 5.0",
  "Framework :: Django :: 5.1",
  "Framework :: Django :: 5.2",
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
]
dependencies = [
  "django>=3.2",
]
optional-dependencies.dev = [
  "django-stubs[compatible-mypy]>=5",
  "mypy>=1.10",
  "playwright>=1.41",
  "pre-commit>=3.6",
  "pytest>=8",
  "pytest-cov>=5",
  "pytest-django>=4.8",
  "pytest-playwright>=0.5",
  "ruff>=0.2",
  "tox>=4",
  "tox-uv>=1",
]
urls.Homepage = "https://django-form-alpine.readthedocs.io/en/latest/"
urls.Issues = "https://github.com/rodolvbg/django-form-alpine/issues"
urls.Repository = "https://github.com/rodolvbg/django-form-alpine"

[dependency-groups]
dev = [
  "django-stubs[compatible-mypy]>=5",
  "mypy>=1.10",
  "playwright>=1.41",
  "pre-commit>=3.6",
  "pytest>=8",
  "pytest-cov>=5",
  "pytest-django>=4.8",
  "pytest-playwright>=0.5",
  "ruff>=0.2",
  "tox>=4",
  "tox-uv>=1",
]

[tool.hatch]
build.targets.wheel.packages = [ "src/django_form_alpine" ]

[tool.ruff]
target-version = "py39"
line-length = 88
# docs/ is the Sphinx/ReadTheDocs site (out of scope for this tooling
# standardization) and README.md ships as-is on PyPI; ruff's formatter can
# reformat embedded Python code fences in Markdown, which would otherwise
# touch both.
extend-exclude = [ "docs", "README.md" ]
lint.select = [ "B", "DJ", "E", "F", "I", "UP", "W" ]
lint.ignore = [ "E203" ]

[tool.mypy]
mypy_path = "src:example"
# mypy itself only supports checking down to 3.10 (this package's actual
# requires-python floor of 3.9 in [project] is unaffected by this setting).
python_version = "3.10"
warn_redundant_casts = true
warn_unused_ignores = true
plugins = [ "mypy_django_plugin.main" ]

[tool.django-stubs]
django_settings_module = "tests.settings"

[tool.pytest]
ini_options.pythonpath = [ "src", "." ]
ini_options.python_files = [ "*_tests.py", "test_*.py", "tests.py" ]
ini_options.addopts = "--cov=django_form_alpine --cov-report=term-missing"
ini_options.DJANGO_SETTINGS_MODULE = "tests.settings"

[tool.coverage]
run.branch = true
run.source = [ "django_form_alpine" ]
report.show_missing = true
report.skip_covered = true

[tool.tox]
env_list = [
  "py314-dj52",
  "py313-dj51",
  "py312-dj42",
  "py312-dj50",
  "py311-dj41",
  "py310-dj32",
  "py310-dj40",
  "py310-dj50",
  "py310-dj51",
  "py310-dj52",
  "py39-dj32",
  "py39-dj40",
  "py39-dj41",
  "py39-dj42",
]
env_run_base.commands = [ [ "pytest", "tests", "--ignore=tests/e2e", "{posargs}" ] ]
env_run_base.deps = [
  "pytest",
  "pytest-django",
  "pytest-cov",
  { replace = "if", condition = "factor.dj32", then = [ "django>=3.2,<3.3" ], extend = true },
  { replace = "if", condition = "factor.dj40", then = [ "django>=4.0,<4.1" ], extend = true },
  { replace = "if", condition = "factor.dj41", then = [ "django>=4.1,<4.2" ], extend = true },
  { replace = "if", condition = "factor.dj42", then = [ "django>=4.2,<4.3" ], extend = true },
  { replace = "if", condition = "factor.dj50", then = [ "django>=5.0,<5.1" ], extend = true },
  { replace = "if", condition = "factor.dj51", then = [ "django>=5.1,<5.2" ], extend = true },
  { replace = "if", condition = "factor.dj52", then = [ "django>=5.2,<5.3" ], extend = true },
]
env_run_base.package = "wheel"
env_run_base.wheel_build_env = ".pkg"
